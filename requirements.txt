# NASA Intelligence Chat System — Python dependencies
# Install: pip install -r requirements.txt
# Dev extras: pip install -r requirements.txt pytest pytest-cov ruff

# ── Core AI / LLM ─────────────────────────────────────────────────────────
openai>=1.0.0

# ── Vector database ────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── RAG evaluation ─────────────────────────────────────────────────────────
ragas>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0

# ── Data handling ──────────────────────────────────────────────────────────
datasets>=2.14.0
pandas>=2.0.0
numpy>=1.24.0

# ── Streamlit UI ───────────────────────────────────────────────────────────
streamlit>=1.32.0

# ── Configuration ──────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Async compatibility (RAGAS inside Streamlit/uvloop) ────────────────────
nest-asyncio>=1.6.0

# ── Development tools (optional) ───────────────────────────────────────────
# pytest>=7.4.0
# pytest-cov>=4.1.0
# ruff>=0.4.0
# jupyter>=1.0.0
# ipykernel>=6.0.0
